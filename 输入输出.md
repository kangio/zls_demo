可以整理成更像一个**推理仿真/寻优系统的 I/O 定义**，并把“输入”和“可调参数”尽量解耦：

### 输入

**1. 模型描述**

* 模型结构：层数、hidden size、Query Head 数、KV Head 数
* Attention 结构：MHA / GQA / MQA / MLA，稠密 / 稀疏注意力
* MoE 结构：专家数、Top-K、Dense/MoE 层分布
* 数据类型：Weight / Activation / KV dtype

**2. 推理软件栈**

* 推理框架及版本：vLLM / MindIE / 其他
* 加速运行时及版本：CUDA / CANN / 其他
* 集合通信库及版本：NCCL / HCCL / 其他
* 软件栈支持的调度、并行、缓存和 KV 搬运能力边界

**3. 硬件与资源拓扑**

* 计算资源：NPU/GPU 型号、数量、各数据类型有效算力、单卡 HBM 容量与带宽
* 主机资源：CPU、DDR 容量与有效带宽、NUMA 拓扑、PCIe 代际与拓扑
* 存储资源：NVMe / SSD / 远端 KV Store 的容量、顺序/随机读写带宽、IOPS、时延和并发队列深度
* 网络资源：节点、NIC、交换机及链路拓扑；链路协议、有效带宽、基础时延和并发流能力
* 资源连接关系：NPU/GPU、CPU、NUMA、NIC 与存储设备的物理归属和连接关系

**4. 业务负载特征**

* 请求规模特征：请求到达时间、Input / Output Length、会话轮数、轮间隔、单任务模型调用次数和工具调用次数
* 前缀共享特征：共享前缀长度、共享范围、复用次数、热点度和复用间隔
* 前缀演化特征：每轮前缀的新增、修改、截断，以及分支、重试和回退形成的继承关系

**5. 目标与约束**

* TTFT / TPOT P50/P95/P99
* 吞吐、最大可承载 QPS / 并发目标
* 成本、设备数量和功耗约束

---

### 寻优维度（5 大类 / 8 小类）

网络资源与拓扑作为寻优的输入边界和约束，不单独作为可调维度。以下八个维度共同参与全局寻优，不表示串行决策顺序。

#### 一、服务控制层

**1. 服务级调度**

* 请求准入、排队和优先级策略
* 实例选择与 Random / RR / Affinity / Load-aware / Cache-aware 路由
* P / D Pairing 策略
* 多实例负载均衡策略

#### 二、推理执行层

**2. 推理引擎调度**

* `max_num_seqs`
* `max_num_batched_tokens`
* Continuous Batching 等实例内调度策略
* Chunked Prefill 开关及 chunk 大小
* Partial / Long Prefill 并发数及阈值
* MTP / Speculative Decode 开关及相关参数

**3. 并行策略**

* TP / DP / PP
* MoE-TP / MoE-EP
* CP / DCP 等上下文并行方式
* P 侧和 D 侧独立并行配置

**4. KV 复用与缓存策略**

* Prefix Cache 开关
* Block / Chunk / Page 粒度
* Cache admission 策略
* 淘汰策略
* Prefetch 策略
* 本地 / 全局索引与共享策略

#### 三、资源布局层

**5. 部署拓扑与资源亲和**

* 单实例 / 多实例 / P-D 分离等部署模式
* P / D 实例数量、单实例卡数和 P:D 配比
* 实例在节点、超节点和集群间的放置
* 计算实例与 NUMA / NIC / Rail / Fabric 的拓扑亲和

**6. KV 资源池与分层放置**

* Native KV / LMCache / HiCache 等 KV 管理组件选择
* 本地池 / 集群池 / 全局池等池化范围
* HBM / DDR / Local SSD / Remote Store 容量及比例
* Mooncake Store / 3FS 等分布式存储后端选择
* KV 数据放置、迁移触发和目标层级
* 池化与分层方案的容量成本、复用收益和搬运开销

#### 四、数据移动层

**7. 数据流编排**

* KV / Tensor / Checkpoint 等数据对象及传输范围
* Slice 大小、数量和粒度
* UB / RDMA / NVMe-oF / TCP 等 Transfer Backend
* Slice 到 NIC / Rail / Backend 的映射
* 传输并发度、优先级、发送顺序和流水重叠
* 基于链路状态的动态重映射、重试和故障绕行

#### 五、网络承载层

**8. 网络内部承载与调度**

* ECMP / 自适应路由及 Fabric 内部路径选择
* 业务优先级到网络队列和 QoS 的映射
* 不同业务流之间的带宽分配、限速和隔离
* ECN / PFC 等拥塞与流量控制策略
* 链路故障后的网络收敛和内部路径重路由

---

### 输出

**1. 规划方案**

* 计算资源：NPU/GPU 型号、数量和节点数量
* 部署架构：P / D 实例数量、比例和节点放置
* 并行策略：TP / DP / PP / EP
* 调度策略：Batch、Chunked Prefill、路由、排队和 admission control
* KV Cache：HBM / DDR / SSD / Remote 容量及缓存、淘汰、Prefetch 和搬运策略
* 网络：拓扑、链路规格和通信协议
* 存储：容量、带宽和 IOPS 配置
* 成本与功耗

**2. 方案性能表现**

* TTFT / TPOT P50 / P95 / P99、E2E Latency
* 吞吐：req/s、token/s，以及最大可承载 QPS / 并发
* SLO 达标率和超时率
* NPU/GPU、HBM、DDR、网络和存储利用率
* P / D 实例利用率、排队时延、排队长度和负载均衡度
* KV Cache 各层命中率、驻留量、淘汰量、迁移量及 Cache miss 重算量
* 网络带宽、存储带宽和 IOPS 利用率
* Preemption 和重算次数
* 各类资源余量

**3. 瓶颈与寻优结果**

* 主要瓶颈及位置：算力 / HBM 容量 / DDR / PCIe / RDMA / SSD / 调度
* 参数敏感性：各变量对 TTFT / TPOT / 吞吐的影响
* 可行配置空间
* 满足 SLO 的最小资源方案
* 给定资源下的最大吞吐方案
* Top-K 候选方案
* 推荐方案相对当前配置的性能、资源和成本收益

可以进一步压缩成一张架构图里的三栏：

> **输入：模型/算子图 + 软件栈 + 硬件与链路能力 + 业务负载 + 映射约束 + SLO**
> **可调：服务控制（1）+ 推理执行（3）+ 资源布局（2）+ 数据移动（1）+ 网络承载（1）**
> **输出：规划方案 + 方案性能表现 + 瓶颈与寻优结果**

这里最重要的一个边界是：**“业务压力”严格来说既可以是输入，也可以是可调变量。** 如果做容量评估，QPS/并发是输入；如果做系统能力寻优，“最大可承载 QPS/并发”应作为搜索变量或输出。
