# 核心建模逻辑

系统以算子 DAG 为统一中间表示，把一次请求拆成可调度、可放置、可计量的算子与数据依赖：

```text
模型结构 + 请求 shape + 软件栈
              ↓
       Prefill / Decode 算子 DAG
              ↓
算子放置 + 并行切分 + 实例调度
              ↓
┌────────────┬────────────┬────────────┐
│ 算：FLOPs  │ 存：数据移动│ 网：跨设备流│
│ Kernel执行 │ HBM/DDR/SSD │ 链路与排队  │
└────────────┴────────────┴────────────┘
              ↓
关键路径汇总 → TTFT / TPOT / 吞吐 / 资源利用率
```

## 算子级计算与存储建模

* 根据算子类型、张量 shape、dtype 推导 FLOPs、权重读取、Activation/KV 读写和临时空间。
* 根据算子放置设备的有效算力、各级存储带宽与时延，判断 Compute-bound 或 Memory/IO-bound。
* 按调度时间线处理算子依赖、设备争用、缓存命中、异步搬运和流水重叠。

## 网络流级建模

* 每条跨设备张量边生成一条或多条流，字段至少包括 `src`、`dst`、`bytes`、`start_time`、`deadline`、`priority`、`transport`。
* 集合通信算子根据通信算法和拓扑展开为具体 P2P 流，而不是只记录一个聚合带宽值。
* KV 迁移、参数/激活传输、TP/PP/EP 通信和存储访问流统一进入链路模型。
* 按路由路径统计共享链路上的并发、排队、拥塞和完成时间，并将通信完成事件反馈给下游算子。

算、网、存通过同一张事件时间线闭环：算子产生数据访问与网络流，存储和网络的完成时间再约束算子的可执行时间。
